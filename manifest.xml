<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <remote name="stuffnodes-github" alias="origin" fetch="."/>
    <default remote="stuffnodes-github" revision="refs/heads/master"/>

    <!-- Workspace and tools -->
    <project path="tools" name="workspace" dest-branch="master" groups="default">
        <linkfile src="Makefile" dest="Makefile"/>
    </project>

    <!-- API -->
    <project path="core/api" name="api" dest-branch="master" groups="notdefault,core"/>
</manifest>
